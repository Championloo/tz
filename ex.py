import argparse

from typing import Optional

from positions import list_of_strings_positions
from strings import list_of_strings, stop_words


# def get_as_list(strings, stop, positions):
#   words = dict(zip(strings, positions))
#   words_pos = {}
#   for word in words:

# get_as_list(list_of_strings, stop_words, list_of_strings_positions)